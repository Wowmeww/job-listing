@props(['label', 'name'])
<label for="{{ $name }}" class="font-semibold flex items-center gap-2">
    <span class="!bg-white !p-1.5 !inline-block text-gray-950"></span>
    <span> {{ $label }}</span>
</label>
